# Dependencias de desarrollo y pruebas.
# Producción usa solo requirements.txt (Railway no instala este archivo).
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest -q

-r requirements.txt

pytest
httpx           # TestClient de FastAPI (starlette.testclient lo requiere)
ruff==0.16.1  # linter; ver ruff.toml. Fijado para que CI y local coincidan.
